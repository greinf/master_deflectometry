#ifndef ENUMS_H
#define ENUMS_H

enum class Shift_mode {
	four_phase_shift,
	user_defined,
	max_value
};

enum class DisplayMode {
	UserInput,
	Automatic,
	max_value
};


enum class AcquisitionMode {
	UserInput,
	Automatic,
};

#endif // !1
